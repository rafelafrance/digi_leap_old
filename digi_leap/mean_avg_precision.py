"""Calculate the mean average precision for scoring object detection results."""


def mAP(results):
    """Calculate the mean average precision for scoring object detection results."""
    score = 0.0

    from pprint import pp
    pp(results)

    return score
